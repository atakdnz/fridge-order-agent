# Getir Configuration
# These are optional - the agent will prompt for login if needed

# Set to 'true' to run browser in headless mode (no visible window)
HEADLESS=false

# Timeout in seconds for page loads
TIMEOUT=30
